# Defina para apontar o app a outro ambiente (ex.: sandbox/staging do Mercado Pago).
# Inclua o /v1 no final. Sem isso, usa produção por padrão.
EXPO_PUBLIC_API_URL=https://construconnect-api-staging.orionsystem.workers.dev/v1
